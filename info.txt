## git bash terminal
## .git/hooks/post-commit 파일 생성
--
#!/bin/bash

# version.txt 파일에서 현재 버전 값을 읽음
version=$(cat version.txt)

# 버전 값을 '.'으로 분리하여 배열로 저장
IFS='.' read -r -a version_parts <<< "$version"

# 마지막 자리 값을 1 증가시킴
last_index=$((${#version_parts[@]} - 1))
version_parts[$last_index]=$((${version_parts[$last_index]} + 1))

# 증가된 버전 값을 다시 문자열로 결합
new_version=$(IFS='.'; echo "${version_parts[*]}")

# 새로운 버전 값을 version.txt 파일에 저장하고 추가
echo "$new_version" > version.txt
git add version.txt

--
## git bash terminal
chmod +x post-commit
--

npm install supabase --save-dev
npx supabase init # intellij settings에서 y
npx supabase functions new download_file
set SUPABASE_ACCESS_TOKEN=key값 이메일로 받은거
npx supabase functions deploy download_file
#invoke
curl -L -X POST 'https://uzefbkvgsuzmopxjxymz.supabase.co/functions/v1/hello-world' -H 'Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InV6ZWZia3Znc3V6bW9weGp4eW16Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3MjkxNTI5NzQsImV4cCI6MjA0NDcyODk3NH0.Q5eiJ32eZa-cALCVxbUlX9sApoOIlzG6a9iZ3hzcXr4' --data '{"name":"Functions"}'